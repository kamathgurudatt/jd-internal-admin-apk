name: Build & Release APK

on:
  push:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ANDROID_HOME: /usr/local/lib/android/sdk

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install npm dependencies
        run: npm install --legacy-peer-deps

      - name: Copy gradle-wrapper.jar from react-native template
        run: |
          mkdir -p android/gradle/wrapper
          cp node_modules/react-native/template/android/gradle/wrapper/gradle-wrapper.jar \
             android/gradle/wrapper/

      # Patch the included build's settings.gradle.kts:
      #   1) Remove foojay JDK toolchain resolver (breaks in composite context)
      #   2) Keep pluginManagement repos (already there)
      - name: Patch RN plugin settings.gradle.kts
        run: |
          cat > node_modules/@react-native/gradle-plugin/settings.gradle.kts << 'EOF'
          pluginManagement {
              repositories {
                  gradlePluginPortal()
                  google()
                  mavenCentral()
              }
          }
          rootProject.name = "react-native-gradle-plugin"
          EOF

      # Patch build.gradle.kts:
      #   Remove internal Gradle API imports (ModuleRegistry, serviceOf) and the
      #   test-task block that uses them. These only affect test configuration
      #   and cause compilation failure in composite build context.
      - name: Patch RN plugin build.gradle.kts — remove internal Gradle APIs
        run: |
          python3 << 'PYEOF'
          import re, sys

          fname = "node_modules/@react-native/gradle-plugin/build.gradle.kts"
          with open(fname) as f:
              content = f.read()

          # Remove internal API imports
          content = re.sub(r'import org\.gradle\.api\.internal\.classpath\.ModuleRegistry\n', '', content)
          content = re.sub(r'import org\.gradle\.configurationcache\.extensions\.serviceOf\n', '', content)

          # Replace the entire tasks.withType<Test>() block (only affects test config)
          # Pattern: from 'tasks.withType<Test>().configureEach {' to matching closing brace
          content = re.sub(
              r'tasks\.withType<Test>\(\)\.configureEach \{.*?\n\}',
              '',
              content,
              flags=re.DOTALL
          )

          with open(fname, 'w') as f:
              f.write(content)
          print("Patched build.gradle.kts")
          # Show imports remaining
          for line in content.split('\n')[:10]:
              print(line)
          PYEOF

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper/dists
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle*', 'android/**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Generate debug keystore
        working-directory: android/app
        run: |
          keytool -genkey -v -keystore debug.keystore -alias androiddebugkey \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass android -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Rename APK with date
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk \
            jd-admin-v1.0-$(date +%Y%m%d).apk

      - name: Delete existing 'latest' tag & release
        run: |
          RELEASE_ID=$(curl -s \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/latest" \
            | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2)
          [ -n "$RELEASE_ID" ] && curl -s -X DELETE \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/$RELEASE_ID" || true
          git push origin :refs/tags/latest 2>/dev/null || true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: "JD Admin — Latest Build"
          body: |
            **JD Admin APK** — automated build from `main` branch.

            > ⚠️ `BASE_URL` = `http://172.29.137.139:8080` (intranet only).
            > Works on office Wi-Fi. For outside-network access, update `src/api/client.js`.

            ### Install
            1. Download `jd-admin-*.apk` below
            2. Copy to Android device
            3. Settings → Security → **Install unknown apps** → allow Files
            4. Tap the APK to install

            ### First launch
            - Enter **Bearer Token** (from `server.js`)
            - Set a **4-digit PIN**
          files: jd-admin-*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
